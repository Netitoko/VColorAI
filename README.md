# 🎨 VincentColorAI

## Краткое описание проекта

**VincentColorAI** — мобильное Android-приложение для генерации цветовых палитр на основе изображений, текстовых описаний и их комбинации.

Приложение предназначено для дизайнеров, художников и студентов, которым необходимо быстро подбирать гармоничные цветовые схемы для визуальных проектов. Пользователь может создавать палитры, сохранять их в личном профиле и публиковать в общей ленте для просмотра и оценки другими пользователями.

Приложение поддерживает социальные функции (лайки, публичная лента), а также частичную работу в оффлайн-режиме. Для публичных палитр используются только общедоступные URL изображений, что обеспечивает корректное отображение данных на разных устройствах.

---

## ✨ Основные возможности

- **Генерация цветовых палитр**
  - по изображению (из галереи или камеры),
  - по текстовому описанию,
  - в комбинированном режиме (изображение + текст).

- **Работа с палитрами**
  - автоматическое формирование палитры из 5–6 цветов,
  - отображение HEX-кодов цветов,
  - копирование цвета в буфер обмена,
  - сохранение палитр в личном профиле,
  - удаление палитр пользователем.

- **Публичная лента**
  - публикация палитр в общую ленту,
  - просмотр палитр других пользователей,
  - сортировка контента по дате публикации.

- **Социальные функции**
  - лайки и дизлайки палитр,
  - подсчёт количества реакций,
  - просмотр профилей других пользователей,
  - просмотр лайкнутых палитр в профиле.

- **Профиль пользователя**
  - просмотр созданных палитр,
  - просмотр лайкнутых палитр,
  - автоматическая сортировка палитр по дате создания (сначала новые).

- **Оффлайн-режим**
  - ограниченная генерация палитр без подключения к интернету,
  - просмотр ранее загруженных данных,
  - уведомление пользователя об отсутствии интернет-соединения,
  - отображение плейсхолдера при недоступности изображений.

---

## 📁 Описание структуры проекта

Проект имеет модульную структуру и организован по принципу разделения ответственности между пользовательским интерфейсом, бизнес-логикой и вспомогательными компонентами.

### Структура исходного кода

### Пакет `com.example.vcolorai`

| Пакет | Назначение | Основные файлы |
|------|-----------|---------------|
| `ui.activities` | Экраны верхнего уровня | `SplashActivity`, `LoginActivity`, `MainActivity` |
| `ui.fragments` | Основные экраны приложения | `GenerationFragment`, `ProfileFragment`, `PublicFeedFragment` |
| `ui.adapters` | Адаптеры списков | `PublicFeedAdapter`, `PalettesAdapter` |
| `ui.dialogs` | Диалоги и окна | `LoadingDialog`, `FullscreenImageDialog` |
| `ui.views` | Кастомные View | `PaletteView` |
| `generation` | Генерация палитр | `PaletteGenerator`, `TextPaletteGenerator` |
| `net` | Работа с сетью | `ImgBBUploader`, `YandexPaletteClient` |
| `model` | Модели данных | `SavedPalette`, `PublicPalette` |
| `utils` | Вспомогательные классы | `ColorUtils`, `DateTimeUtils` |


### Ресурсы приложения



res/
├── layout/ // XML-разметки экранов, элементов списков и диалогов
├── drawable/ // иконки, плейсхолдеры, векторные ресурсы
├── values/ // цвета, темы, стили, строки
└── xml/ // fileprovider и конфигурационные XML

assets/
└── color_keywords.json // словарь цветовых ключевых слов


---

## 🏗 Архитектура приложения

Приложение построено на основе **Fragment-based архитектуры** с логическим разделением слоёв.

### Общая архитектурная схема



UI слой (Activities / Fragments)
↓
Бизнес-логика (PaletteGenerator, Utils)
↓
Слой данных (Firebase Firestore, Auth, ImgBB)


### Архитектурные решения

- каждый экран реализован в виде `Fragment`;
- навигация осуществляется через `MainActivity`;
- асинхронные операции выполняются с использованием **Kotlin Coroutines**;
- сетевые и вычислительно затратные операции выполняются вне UI-потока;
- работа с Firebase осуществляется напрямую во фрагментах (допустимо для учебного проекта).

В дальнейшем архитектура может быть расширена с использованием Repository и ViewModel слоёв.

---

## 🗄 Структура базы данных (Firestore)

Для хранения данных используется **Firebase Cloud Firestore**.

### Основные коллекции

#### `users`
```json
{
  "uid": "string",
  "email": "string",
  "username": "string",
  "createdAt": "timestamp"
}

color_palettes
{
  "id": "string",
  "userId": "string",
  "paletteName": "string",
  "colors": ["#FFFFFF", "#000000"],
  "sourceType": "text | image | combined",
  "imageUri": "https://... | null",
  "isPublic": true,
  "likesCount": 0,
  "creationDate": 1710000000000
}

liked_palettes (подколлекция пользователя)
{
  "paletteId": "string",
  "createdAt": "timestamp"
}
```

Особенности работы с данными

в базе данных хранятся только публичные URL изображений;

локальные URI (content://, file://) не сохраняются;

при удалении палитры выполняется синхронизация пользовательских лайков;

палитры в профиле сортируются по дате создания (сначала новые).

🛠 Используемые технологии

Kotlin

Android SDK

Firebase Authentication
Yandex GPT API

Glide

Kotlin Coroutines

Android Palette API

📌 Статус проекта

Проект реализован в рамках учебной дисциплины и соответствует заявленным функциональным требованиям.
Возможные направления дальнейшего развития включают расширенный оффлайн-режим, экспорт палитр и улучшение архитектуры данных.
